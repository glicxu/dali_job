# Leave this unset for local development so DaliJob automatically uses the
# same localhost or 127.0.0.1 hostname as the browser. Set an exact API URL
# for deployed environments.
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com/api/v1
